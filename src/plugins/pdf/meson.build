shared_library('pdf',
    'pdf_plugin.c',
    dependencies: [glib],
    include_directories: [
        include_directories('../../plugins/sdk'),
    ],
    name_prefix: '',
    install: true,
    install_dir: get_option('libdir') / 'malky' / 'plugins',
)
